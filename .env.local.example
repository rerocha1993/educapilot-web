# Copie para .env.local (não versionado) e ajuste conforme o ambiente.
# Sem /api no final — os paths do cliente gerado (ex: /api/Auth/login) já incluem o
# prefixo, igual as rotas reais dos controllers ([Route("api/[controller]")]).
NEXT_PUBLIC_API_BASE_URL=https://localhost:7141
